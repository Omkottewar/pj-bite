import { Loader2 } from "lucide-react";

export default function Loading() {
  // Disabled centralized loader to let TopProgressBar and custom Splash Screen handle navigation UX without violently replacing the page content.
  return null;
}
